#lang setup/infotab

(define collection 'multi)
(define version "1.0.1") ; version bump for small API change

(define deps
  (list "base"))

(define build-deps
  (list "rackunit-lib"
        "scribble-lib"))

